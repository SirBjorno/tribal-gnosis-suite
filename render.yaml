services:
  - type: web
    name: tribal-gnosis-live
    env: node
    buildCommand: npm run build:prod
    startCommand: npm run serve
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false # This will be set manually in Render dashboard
    healthCheckPath: /